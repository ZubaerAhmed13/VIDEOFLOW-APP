# UX Step 2 certification helper

Only `finalize_step2_certification_metadata.py` is retained here. It converts the checked-in completion-report template into the runtime-bound exact-head report artifact after all automated Step-2 certification jobs pass.
